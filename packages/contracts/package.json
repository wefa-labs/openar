{
  "name": "contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm recursive run dev",
    "deploy": "pnpm deploy:checkers && pnpm deploy:tictactoe && pnpm deploy:openar",
    "deploy:openar": "cd openar && pnpm run deploy --profile=lattice-testnet --priorityFeeMultiplier=3",
    "deploy:checkers": "cd checkers && pnpm run deploy --profile=optimism-goerli",
    "deploy:tictactoe": "cd tictactoe && pnpm run deploy --profile=optimism-goerli",
    "deploy:testnet": "pnpm recursive run deploy --profile=lattice-testnet",
    "deploy:optimism-testnet": "pnpm recursive run deploy --profile=optimism-goerli",
    "deploy:optimism": "pnpm recursive run deploy --profile=optimism",
    "lint": "pnpm run prettier && pnpm run solhint",
    "prettier": "prettier --write 'src/**/*.sol'",
    "solhint": "solhint --config ./.solhint.json 'src/**/*.sol' --fix"
  },
  "devDependencies": {
    "@typechain/ethers-v5": "^10.2.0",
    "@types/node": "^18.15.11",
    "ds-test": "https://github.com/dapphub/ds-test.git#c9ce3f25bde29fc5eb9901842bf02850dfd2d084",
    "forge-std": "https://github.com/foundry-rs/forge-std.git#b4f121555729b3afb3c5ffccb62ff4b6e2818fd3",
    "typechain": "^8.1.1"
  }
}
