// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

/* Autogenerated file. Do not edit manually. */

// Import schema type
import { SchemaType } from "@latticexyz/schema-type/src/solidity/SchemaType.sol";

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { Schema, SchemaLib } from "@latticexyz/store/src/Schema.sol";
import { PackedCounter, PackedCounterLib } from "@latticexyz/store/src/PackedCounter.sol";

bytes32 constant _tableId = bytes32(abi.encodePacked(bytes16("tictactoe"), bytes16("Match")));
bytes32 constant MatchTableId = _tableId;

struct MatchData {
  uint8 turnCount;
  bytes32 id;
  uint8 spacePosition;
  bytes32 spaceId;
  address currentPlayer;
  address winner;
  address[2] players;
  uint8[9] board;
}

library Match {
  /** Get the table's schema */
  function getSchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](8);
    _schema[0] = SchemaType.UINT8;
    _schema[1] = SchemaType.BYTES32;
    _schema[2] = SchemaType.UINT8;
    _schema[3] = SchemaType.BYTES32;
    _schema[4] = SchemaType.ADDRESS;
    _schema[5] = SchemaType.ADDRESS;
    _schema[6] = SchemaType.ADDRESS_ARRAY;
    _schema[7] = SchemaType.UINT8_ARRAY;

    return SchemaLib.encode(_schema);
  }

  function getKeySchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](2);
    _schema[0] = SchemaType.BYTES32;
    _schema[1] = SchemaType.UINT8;

    return SchemaLib.encode(_schema);
  }

  /** Get the table's metadata */
  function getMetadata() internal pure returns (string memory, string[] memory) {
    string[] memory _fieldNames = new string[](8);
    _fieldNames[0] = "turnCount";
    _fieldNames[1] = "id";
    _fieldNames[2] = "spacePosition";
    _fieldNames[3] = "spaceId";
    _fieldNames[4] = "currentPlayer";
    _fieldNames[5] = "winner";
    _fieldNames[6] = "players";
    _fieldNames[7] = "board";
    return ("Match", _fieldNames);
  }

  /** Register the table's schema */
  function registerSchema() internal {
    StoreSwitch.registerSchema(_tableId, getSchema(), getKeySchema());
  }

  /** Register the table's schema (using the specified store) */
  function registerSchema(IStore _store) internal {
    _store.registerSchema(_tableId, getSchema(), getKeySchema());
  }

  /** Set the table's metadata */
  function setMetadata() internal {
    (string memory _tableName, string[] memory _fieldNames) = getMetadata();
    StoreSwitch.setMetadata(_tableId, _tableName, _fieldNames);
  }

  /** Set the table's metadata (using the specified store) */
  function setMetadata(IStore _store) internal {
    (string memory _tableName, string[] memory _fieldNames) = getMetadata();
    _store.setMetadata(_tableId, _tableName, _fieldNames);
  }

  /** Get turnCount */
  function getTurnCount(bytes32 matchId, uint8 position) internal view returns (uint8 turnCount) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 0);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Get turnCount (using the specified store) */
  function getTurnCount(IStore _store, bytes32 matchId, uint8 position) internal view returns (uint8 turnCount) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 0);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Set turnCount */
  function setTurnCount(bytes32 matchId, uint8 position, uint8 turnCount) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 0, abi.encodePacked((turnCount)));
  }

  /** Set turnCount (using the specified store) */
  function setTurnCount(IStore _store, bytes32 matchId, uint8 position, uint8 turnCount) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 0, abi.encodePacked((turnCount)));
  }

  /** Get id */
  function getId(bytes32 matchId, uint8 position) internal view returns (bytes32 id) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 1);
    return (Bytes.slice32(_blob, 0));
  }

  /** Get id (using the specified store) */
  function getId(IStore _store, bytes32 matchId, uint8 position) internal view returns (bytes32 id) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 1);
    return (Bytes.slice32(_blob, 0));
  }

  /** Set id */
  function setId(bytes32 matchId, uint8 position, bytes32 id) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 1, abi.encodePacked((id)));
  }

  /** Set id (using the specified store) */
  function setId(IStore _store, bytes32 matchId, uint8 position, bytes32 id) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 1, abi.encodePacked((id)));
  }

  /** Get spacePosition */
  function getSpacePosition(bytes32 matchId, uint8 position) internal view returns (uint8 spacePosition) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 2);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Get spacePosition (using the specified store) */
  function getSpacePosition(
    IStore _store,
    bytes32 matchId,
    uint8 position
  ) internal view returns (uint8 spacePosition) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 2);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Set spacePosition */
  function setSpacePosition(bytes32 matchId, uint8 position, uint8 spacePosition) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 2, abi.encodePacked((spacePosition)));
  }

  /** Set spacePosition (using the specified store) */
  function setSpacePosition(IStore _store, bytes32 matchId, uint8 position, uint8 spacePosition) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 2, abi.encodePacked((spacePosition)));
  }

  /** Get spaceId */
  function getSpaceId(bytes32 matchId, uint8 position) internal view returns (bytes32 spaceId) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 3);
    return (Bytes.slice32(_blob, 0));
  }

  /** Get spaceId (using the specified store) */
  function getSpaceId(IStore _store, bytes32 matchId, uint8 position) internal view returns (bytes32 spaceId) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 3);
    return (Bytes.slice32(_blob, 0));
  }

  /** Set spaceId */
  function setSpaceId(bytes32 matchId, uint8 position, bytes32 spaceId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 3, abi.encodePacked((spaceId)));
  }

  /** Set spaceId (using the specified store) */
  function setSpaceId(IStore _store, bytes32 matchId, uint8 position, bytes32 spaceId) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 3, abi.encodePacked((spaceId)));
  }

  /** Get currentPlayer */
  function getCurrentPlayer(bytes32 matchId, uint8 position) internal view returns (address currentPlayer) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 4);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Get currentPlayer (using the specified store) */
  function getCurrentPlayer(
    IStore _store,
    bytes32 matchId,
    uint8 position
  ) internal view returns (address currentPlayer) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 4);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Set currentPlayer */
  function setCurrentPlayer(bytes32 matchId, uint8 position, address currentPlayer) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 4, abi.encodePacked((currentPlayer)));
  }

  /** Set currentPlayer (using the specified store) */
  function setCurrentPlayer(IStore _store, bytes32 matchId, uint8 position, address currentPlayer) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 4, abi.encodePacked((currentPlayer)));
  }

  /** Get winner */
  function getWinner(bytes32 matchId, uint8 position) internal view returns (address winner) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 5);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Get winner (using the specified store) */
  function getWinner(IStore _store, bytes32 matchId, uint8 position) internal view returns (address winner) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 5);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Set winner */
  function setWinner(bytes32 matchId, uint8 position, address winner) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 5, abi.encodePacked((winner)));
  }

  /** Set winner (using the specified store) */
  function setWinner(IStore _store, bytes32 matchId, uint8 position, address winner) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 5, abi.encodePacked((winner)));
  }

  /** Get players */
  function getPlayers(bytes32 matchId, uint8 position) internal view returns (address[2] memory players) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 6);
    return toStaticArray_address_2(SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_address());
  }

  /** Get players (using the specified store) */
  function getPlayers(
    IStore _store,
    bytes32 matchId,
    uint8 position
  ) internal view returns (address[2] memory players) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 6);
    return toStaticArray_address_2(SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_address());
  }

  /** Set players */
  function setPlayers(bytes32 matchId, uint8 position, address[2] memory players) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 6, EncodeArray.encode(fromStaticArray_address_2(players)));
  }

  /** Set players (using the specified store) */
  function setPlayers(IStore _store, bytes32 matchId, uint8 position, address[2] memory players) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 6, EncodeArray.encode(fromStaticArray_address_2(players)));
  }

  /** Get the length of players */
  function lengthPlayers(bytes32 matchId, uint8 position) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    uint256 _byteLength = StoreSwitch.getFieldLength(_tableId, _keyTuple, 6, getSchema());
    return _byteLength / 20;
  }

  /** Get the length of players (using the specified store) */
  function lengthPlayers(IStore _store, bytes32 matchId, uint8 position) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    uint256 _byteLength = _store.getFieldLength(_tableId, _keyTuple, 6, getSchema());
    return _byteLength / 20;
  }

  /** Get an item of players (unchecked, returns invalid data if index overflows) */
  function getItemPlayers(bytes32 matchId, uint8 position, uint256 _index) internal view returns (address) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getFieldSlice(_tableId, _keyTuple, 6, getSchema(), _index * 20, (_index + 1) * 20);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Get an item of players (using the specified store) (unchecked, returns invalid data if index overflows) */
  function getItemPlayers(
    IStore _store,
    bytes32 matchId,
    uint8 position,
    uint256 _index
  ) internal view returns (address) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getFieldSlice(_tableId, _keyTuple, 6, getSchema(), _index * 20, (_index + 1) * 20);
    return (address(Bytes.slice20(_blob, 0)));
  }

  /** Push an element to players */
  function pushPlayers(bytes32 matchId, uint8 position, address _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.pushToField(_tableId, _keyTuple, 6, abi.encodePacked((_element)));
  }

  /** Push an element to players (using the specified store) */
  function pushPlayers(IStore _store, bytes32 matchId, uint8 position, address _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.pushToField(_tableId, _keyTuple, 6, abi.encodePacked((_element)));
  }

  /** Pop an element from players */
  function popPlayers(bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.popFromField(_tableId, _keyTuple, 6, 20);
  }

  /** Pop an element from players (using the specified store) */
  function popPlayers(IStore _store, bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.popFromField(_tableId, _keyTuple, 6, 20);
  }

  /** Update an element of players at `_index` */
  function updatePlayers(bytes32 matchId, uint8 position, uint256 _index, address _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.updateInField(_tableId, _keyTuple, 6, _index * 20, abi.encodePacked((_element)));
  }

  /** Update an element of players (using the specified store) at `_index` */
  function updatePlayers(IStore _store, bytes32 matchId, uint8 position, uint256 _index, address _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.updateInField(_tableId, _keyTuple, 6, _index * 20, abi.encodePacked((_element)));
  }

  /** Get board */
  function getBoard(bytes32 matchId, uint8 position) internal view returns (uint8[9] memory board) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 7);
    return toStaticArray_uint8_9(SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_uint8());
  }

  /** Get board (using the specified store) */
  function getBoard(IStore _store, bytes32 matchId, uint8 position) internal view returns (uint8[9] memory board) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 7);
    return toStaticArray_uint8_9(SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_uint8());
  }

  /** Set board */
  function setBoard(bytes32 matchId, uint8 position, uint8[9] memory board) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setField(_tableId, _keyTuple, 7, EncodeArray.encode(fromStaticArray_uint8_9(board)));
  }

  /** Set board (using the specified store) */
  function setBoard(IStore _store, bytes32 matchId, uint8 position, uint8[9] memory board) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setField(_tableId, _keyTuple, 7, EncodeArray.encode(fromStaticArray_uint8_9(board)));
  }

  /** Get the length of board */
  function lengthBoard(bytes32 matchId, uint8 position) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    uint256 _byteLength = StoreSwitch.getFieldLength(_tableId, _keyTuple, 7, getSchema());
    return _byteLength / 1;
  }

  /** Get the length of board (using the specified store) */
  function lengthBoard(IStore _store, bytes32 matchId, uint8 position) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    uint256 _byteLength = _store.getFieldLength(_tableId, _keyTuple, 7, getSchema());
    return _byteLength / 1;
  }

  /** Get an item of board (unchecked, returns invalid data if index overflows) */
  function getItemBoard(bytes32 matchId, uint8 position, uint256 _index) internal view returns (uint8) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getFieldSlice(_tableId, _keyTuple, 7, getSchema(), _index * 1, (_index + 1) * 1);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Get an item of board (using the specified store) (unchecked, returns invalid data if index overflows) */
  function getItemBoard(IStore _store, bytes32 matchId, uint8 position, uint256 _index) internal view returns (uint8) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getFieldSlice(_tableId, _keyTuple, 7, getSchema(), _index * 1, (_index + 1) * 1);
    return (uint8(Bytes.slice1(_blob, 0)));
  }

  /** Push an element to board */
  function pushBoard(bytes32 matchId, uint8 position, uint8 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.pushToField(_tableId, _keyTuple, 7, abi.encodePacked((_element)));
  }

  /** Push an element to board (using the specified store) */
  function pushBoard(IStore _store, bytes32 matchId, uint8 position, uint8 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.pushToField(_tableId, _keyTuple, 7, abi.encodePacked((_element)));
  }

  /** Pop an element from board */
  function popBoard(bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.popFromField(_tableId, _keyTuple, 7, 1);
  }

  /** Pop an element from board (using the specified store) */
  function popBoard(IStore _store, bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.popFromField(_tableId, _keyTuple, 7, 1);
  }

  /** Update an element of board at `_index` */
  function updateBoard(bytes32 matchId, uint8 position, uint256 _index, uint8 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.updateInField(_tableId, _keyTuple, 7, _index * 1, abi.encodePacked((_element)));
  }

  /** Update an element of board (using the specified store) at `_index` */
  function updateBoard(IStore _store, bytes32 matchId, uint8 position, uint256 _index, uint8 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.updateInField(_tableId, _keyTuple, 7, _index * 1, abi.encodePacked((_element)));
  }

  /** Get the full data */
  function get(bytes32 matchId, uint8 position) internal view returns (MatchData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = StoreSwitch.getRecord(_tableId, _keyTuple, getSchema());
    return decode(_blob);
  }

  /** Get the full data (using the specified store) */
  function get(IStore _store, bytes32 matchId, uint8 position) internal view returns (MatchData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    bytes memory _blob = _store.getRecord(_tableId, _keyTuple, getSchema());
    return decode(_blob);
  }

  /** Set the full data using individual values */
  function set(
    bytes32 matchId,
    uint8 position,
    uint8 turnCount,
    bytes32 id,
    uint8 spacePosition,
    bytes32 spaceId,
    address currentPlayer,
    address winner,
    address[2] memory players,
    uint8[9] memory board
  ) internal {
    bytes memory _data = encode(turnCount, id, spacePosition, spaceId, currentPlayer, winner, players, board);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.setRecord(_tableId, _keyTuple, _data);
  }

  /** Set the full data using individual values (using the specified store) */
  function set(
    IStore _store,
    bytes32 matchId,
    uint8 position,
    uint8 turnCount,
    bytes32 id,
    uint8 spacePosition,
    bytes32 spaceId,
    address currentPlayer,
    address winner,
    address[2] memory players,
    uint8[9] memory board
  ) internal {
    bytes memory _data = encode(turnCount, id, spacePosition, spaceId, currentPlayer, winner, players, board);

    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.setRecord(_tableId, _keyTuple, _data);
  }

  /** Set the full data using the data struct */
  function set(bytes32 matchId, uint8 position, MatchData memory _table) internal {
    set(
      matchId,
      position,
      _table.turnCount,
      _table.id,
      _table.spacePosition,
      _table.spaceId,
      _table.currentPlayer,
      _table.winner,
      _table.players,
      _table.board
    );
  }

  /** Set the full data using the data struct (using the specified store) */
  function set(IStore _store, bytes32 matchId, uint8 position, MatchData memory _table) internal {
    set(
      _store,
      matchId,
      position,
      _table.turnCount,
      _table.id,
      _table.spacePosition,
      _table.spaceId,
      _table.currentPlayer,
      _table.winner,
      _table.players,
      _table.board
    );
  }

  /** Decode the tightly packed blob using this table's schema */
  function decode(bytes memory _blob) internal view returns (MatchData memory _table) {
    // 106 is the total byte length of static data
    PackedCounter _encodedLengths = PackedCounter.wrap(Bytes.slice32(_blob, 106));

    _table.turnCount = (uint8(Bytes.slice1(_blob, 0)));

    _table.id = (Bytes.slice32(_blob, 1));

    _table.spacePosition = (uint8(Bytes.slice1(_blob, 33)));

    _table.spaceId = (Bytes.slice32(_blob, 34));

    _table.currentPlayer = (address(Bytes.slice20(_blob, 66)));

    _table.winner = (address(Bytes.slice20(_blob, 86)));

    // Store trims the blob if dynamic fields are all empty
    if (_blob.length > 106) {
      uint256 _start;
      // skip static data length + dynamic lengths word
      uint256 _end = 138;

      _start = _end;
      _end += _encodedLengths.atIndex(0);
      _table.players = toStaticArray_address_2(SliceLib.getSubslice(_blob, _start, _end).decodeArray_address());

      _start = _end;
      _end += _encodedLengths.atIndex(1);
      _table.board = toStaticArray_uint8_9(SliceLib.getSubslice(_blob, _start, _end).decodeArray_uint8());
    }
  }

  /** Tightly pack full data using this table's schema */
  function encode(
    uint8 turnCount,
    bytes32 id,
    uint8 spacePosition,
    bytes32 spaceId,
    address currentPlayer,
    address winner,
    address[2] memory players,
    uint8[9] memory board
  ) internal view returns (bytes memory) {
    uint40[] memory _counters = new uint40[](2);
    _counters[0] = uint40(players.length * 20);
    _counters[1] = uint40(board.length * 1);
    PackedCounter _encodedLengths = PackedCounterLib.pack(_counters);

    return
      abi.encodePacked(
        turnCount,
        id,
        spacePosition,
        spaceId,
        currentPlayer,
        winner,
        _encodedLengths.unwrap(),
        EncodeArray.encode(fromStaticArray_address_2(players)),
        EncodeArray.encode(fromStaticArray_uint8_9(board))
      );
  }

  /** Encode keys as a bytes32 array using this table's schema */
  function encodeKeyTuple(bytes32 matchId, uint8 position) internal pure returns (bytes32[] memory _keyTuple) {
    _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));
  }

  /* Delete all data for given keys */
  function deleteRecord(bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /* Delete all data for given keys (using the specified store) */
  function deleteRecord(IStore _store, bytes32 matchId, uint8 position) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32((matchId));
    _keyTuple[1] = bytes32(uint256((position)));

    _store.deleteRecord(_tableId, _keyTuple);
  }
}

function toStaticArray_address_2(address[] memory _value) pure returns (address[2] memory _result) {
  // in memory static arrays are just dynamic arrays without the length byte
  assembly {
    _result := add(_value, 0x20)
  }
}

function toStaticArray_uint8_9(uint8[] memory _value) pure returns (uint8[9] memory _result) {
  // in memory static arrays are just dynamic arrays without the length byte
  assembly {
    _result := add(_value, 0x20)
  }
}

function fromStaticArray_address_2(address[2] memory _value) view returns (address[] memory _result) {
  _result = new address[](2);
  uint256 fromPointer;
  uint256 toPointer;
  assembly {
    fromPointer := _value
    toPointer := add(_result, 0x20)
  }
  Memory.copy(fromPointer, toPointer, 64);
}

function fromStaticArray_uint8_9(uint8[9] memory _value) view returns (uint8[] memory _result) {
  _result = new uint8[](9);
  uint256 fromPointer;
  uint256 toPointer;
  assembly {
    fromPointer := _value
    toPointer := add(_result, 0x20)
  }
  Memory.copy(fromPointer, toPointer, 288);
}
